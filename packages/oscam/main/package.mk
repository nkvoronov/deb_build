# Имя пакета
PKG_NAME="oscam"

# Версия пакета
PKG_VERSION="1.20"

# Эпоха (по умолчанию "none")
PKG_EPOCH="none"

# Ревизия debian (по умолчанию "none")
PKG_DEBIAN_REV="1"

# Тип получаемых исходников (git,svn,hg,web)
PKG_SOURCE_TYPE="svn"

# Путь к исходникам
PKG_SOURCE_DEST="http://www.streamboard.tv/svn/oscam/trunk"

# Тип версии исходников (none,branch,tags,revision)
PKG_SOURCE_TREE_VERSION_TYPE="none"

# Название для типа версии (если пусто то ("none"))
PKG_SOURCE_TREE_VERSION_NAME="none"

# Имя папки в архиве (только для web но не пустой ("none"))
PKG_SOURCE_DIR="none"

# Вариант сборки
PKG_PPA="ppa1"

# Имя ресурса на launchpad
PKG_LAUNCHPAD_REPOSITORY="oscam"

# Очистка CVS (.git, .svn, .hg)
PKG_CVS_CLEAR="no"
